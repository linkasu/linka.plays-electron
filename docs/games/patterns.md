# Паттерны

> Registry-backed spec. Документ создан из текущего Electron registry и audit-данных; при продуктовой доработке его нужно расширять вручную.

## Registry

| Поле | Значение |
|---|---|
| ID | `patterns` |
| Route | `/games/patterns` |
| Категория | `sequencing` — Последовательности |
| Status | `polished` |
| Resolved stability | `publish` |
| Readiness group | `ready` |
| Skills | последовательность, выбор |
| Recommended session | 120 сек |
| Min target size | 150 px |
| Default dwell | 1200 ms |

## Назначение

Ребёнок смотрит на ряд фигур и выбирает, какая фигура продолжает повтор. Игра тренирует распознавание ABAB, AAB и ABCABC последовательностей.

Самостоятельная формулировка для меню: Продолжи ряд.

## Игровой цикл

```text
показ ряда с пустым местом -> выбор продолжения -> feedback -> следующий раунд или результат
```

## Управление взглядом

- Основной ввод должен быть gaze-first; mouse fallback сохраняется для отладки и занятий без трекера.
- Минимальный целевой размер из registry: 150 px.
- Базовый dwell из registry: 1200 ms.
- Последний Electron CDP audit: failures = 0, max targets = 4, min visible targets = 4.
- Минимальная short-side эвристика targets: 0.178.

## Дефектологическая ценность

- тренирует понимание порядка и причинности
- поддерживает пошаговую инструкцию
- снижает нагрузку через крупный выбор

## Метрики и сессия

- Сессия должна использовать общий game session flow и завершаться через результат для взрослого.
- Важные метрики: успешные выборы, ошибки/отмены, dwell time, подсказки, потеря валидного взгляда.
- Рекомендуемая длительность занятия: 120 сек.

## Реализация

| Проверка | Состояние |
|---|---|
| Route в router | есть |
| Vue-компонент | `PatternsGame.vue` |
| Model | `model.ts` |
| Model test | `model.test.ts` |
| Audio module | `audio.ts` |
| Runtime audit doc | `docs/tests/2026-06-10/patterns.md` |

## Audio/TTS

- Игра включает тихий звук по умолчанию.
- TTS проговаривает только общую задачу и нейтральный feedback.
- Ошибка не раскрывает правильную фигуру и не подсвечивает весь шаблон как готовую подсказку.
- Пока звучит TTS/feedback, выбор заблокирован.

## Готовность

Игра находится в группе `needs-check` до ручного approve в очереди ревью.

Автоматические blockers:

- нет

## QA checklist

- Route открывается в Electron, не только в обычном браузере.
- На 800×600 и 1024×600 основные цели видны без ручного скролла.
- HUD, prompt и overlays не перекрывают активные цели.
- Для canvas/fullscreen игры PNG подтверждает непустую сцену и читаемые active zones.
- Звук, если есть, остаётся опциональным и не ломает gameplay при ошибке загрузки.
- Для игр с правилами model tests покрывают правильный ответ, ошибки и завершение раунда.
- Ошибка не должна раскрывать правильный вариант.
- Модель должна использовать контролируемый random для тестируемости.

## Next step

Оставить в ready-очереди и проверять регрессии через общий Electron CDP audit.
